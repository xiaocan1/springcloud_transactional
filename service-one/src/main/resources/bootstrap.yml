spring:
  application:
    name: a
  cloud:
    config:
      uri: http://127.0.0.1:8081
      enabled: true
      profile: dev
      label: ""

server:
  port: 8082
  context-path: /
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8080/eureka/